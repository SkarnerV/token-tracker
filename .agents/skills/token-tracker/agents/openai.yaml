interface:
  display_name: "Token Tracker"
  short_description: "Repo-specific patterns and workflows for token-tracker"
  default_prompt: "Use the token-tracker repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true